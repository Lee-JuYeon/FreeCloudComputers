# secrets — never commit provider tokens
.env
*.env
kaggle.json
# local state: cooldown db, run logs, pulled checkpoints, 로그인 쿠키/시크릿
# ⚠️ gitignore는 줄끝 주석을 지원하지 않는다 — 같은 줄에 주석을 붙이면 패턴 전체가
#    리터럴이 되어 아무것도 무시하지 않는다(실측: secrets.json이 노출돼 있었음).
.freecloud/

# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/

# artifacts
artifacts/
checkpoints/

# 로컬 실행 로그
*.log
